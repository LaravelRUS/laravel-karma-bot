filter:
  excluded_paths:
    - "docker/"
    - "karmabot/tests/"
build:
    environment:
        php:
            version: 7.1.0
    tests:
        override:
            -
                command: 'phpunit --coverage-clover=phpunit.xml'
                coverage:
                    file: 'phpunit.xml'
                    format: 'clover'
